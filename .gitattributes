# Normalize every text file to LF in the repository and on checkout, so the
# lint gate behaves the same on every OS. Binary assets are left untouched.
* text=auto eol=lf

*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
